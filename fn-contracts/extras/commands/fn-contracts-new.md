Define a new **functional contract** (`fun interface` + `operator fun invoke`) in the shared/common `functions` package. Group it under an existing or new `<Domain>Functions` object. Use domain types only — no HTTP or framework types. Name the contract as a verb or verb-noun (e.g. `GetDevice`, `CreateOwnership`). Then scaffold the first implementation as a `@Service` (ask which type: LocalDb, Client, Controller, or plain Service). Wire with constructor injection.
