Add or update `specs/<slug>/ui.md` for frontend/app developers consuming this backend: auth and path context (e.g. `workshopId`), screens mapped to HTTP verbs/paths at a high level, dynamic behaviour (schemas, pagination), errors and empty states, refetch after mutations. No OpenAPI paste unless we add a link. Use the feature we’re discussing or ask for the slug. Update `SPEC.md` Changelog and link `ui.md` from `SPEC.md` if missing.
